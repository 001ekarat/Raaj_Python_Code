# Exception Handling :- Exception is nothing but runtime errors and it occurs due to incometimplementation of logic.
# It is a mechanism through which we can handles the runtime errors.
# Note:- try , except, else

a = int(input("Enter First No: "))
b = int(input("Enter second No: "))

