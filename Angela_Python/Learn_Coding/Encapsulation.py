# Encapsulation :- python provides access to all the variable and methods globally.
# By using encapsulation , we can restrict the variable and methods acces globally by making or protected.
# Note Single underscore(protected) (2) double underscore (Private)
