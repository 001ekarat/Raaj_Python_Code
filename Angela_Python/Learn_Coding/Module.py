# Module are nothing but group of functions, variable and classes that are saved to a file .
# Note:_    1) Code Reusebility
#           2) If python file (first.py) called then its module name should be first

# Types 1) Pre-defined 2) User-defined

import calendar, keyword
print(calendar.month(2024,10))
print(keyword.kwlist)
